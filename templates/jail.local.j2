# file: jail.local
# This configuration file overrides the settings in jail.conf

# The DEFAULT allows a global definition of the options. They can be override
# in each jail afterwards.

[INCLUDES]

#before = paths-distro.conf
before = paths-{{ ansible_distribution|lower }}.conf

[DEFAULT]

{% for key, value in fail2ban_jail_local_default.iteritems() %}
{{ key }} = {{ value }}
{% endfor %}

{# [{{ jails }}] #}
{% for jails in fail2ban_jails %}
{{ jails }}
{% endfor %}

{#
{% for key, value in jails %}
{{ key }} = {{ value }}
{% endfor %}
#}
